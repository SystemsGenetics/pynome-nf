process {
  // General settings that apply to all processes. These can be overridden
  // by each proces individually if needed.
  container = "pynome/pynome:0.9999"
  errorStrategy = "retry"
  maxRetries = 0
  memory = '4 GB'
}
